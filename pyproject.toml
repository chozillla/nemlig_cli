[project]
name = "nemlig-cli"
version = "0.3.0"
description = "CLI for nemlig.com grocery shopping (Danish online grocery store)"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "argcomplete>=3.0.0",
]

[project.scripts]
nemlig = "nemlig_cli:main"
